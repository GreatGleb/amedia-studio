# План: Обновление Главного Экрана и Блока Анимации

Этот план описывает техническую реализацию изменений, описанных в `ai/plans/hero-update.md`.

## Цель
Разделить текущий полноэкранный `HeroCanvas` на два блока: классический Hero-блок с CTA и информативный блок с анимацией услуг ниже по странице.

## Вопросы к пользователю
- **Фоновое изображение**: У тебя есть готовая картинка для фона Hero или мне использовать плейсхолдер/сгенерировать подходящее изображение?

## Предлагаемые изменения

### 1. Компонент Hero (Главный экран)

#### [MODIFY] `index.tsx`(file:///c:/Users/great/Documents/bussiness/Amedia/website_public/src/components/sections/hero/index.tsx)
- Удалить вызов `HeroCanvas`.
- Изменить верстку: сделать стандартный экран `100vh`.
- Добавить фоновое изображение (или градиент как плейсхолдер).
- Добавить яркий заголовок, подзаголовок и кнопку CTA (Call to Action).

### 2. Новый компонент ServicesSequence (Блок услуг)

#### [NEW] `services-sequence.tsx`(file:///c:/Users/great/Documents/bussiness/Amedia/website_public/src/components/sections/hero/services-sequence.tsx)
- Создать новый компонент, который будет содержать:
  - Слева: Список услуг (Performance, Branding, Creative, Strategy).
  - Справа: Липкий (sticky) блок с Canvas анимацией (используя `useHeroSequence`).
- Логика: При скролле текст слева плавно сменяется или подсвечивается, а Canvas справа проигрывает кадры.

### 3. Главная страница

#### [MODIFY] `page.tsx`(file:///c:/Users/great/Documents/bussiness/Amedia/website_public/src/app/page.tsx)
- Импортировать и добавить `ServicesSequence` сразу после `Hero`.

## План верификации
- Проверка адаптивности: как блок "текст + канвас" ведет себя на мобильных устройствах (вероятно, на мобильных они должны идти друг за другом).
- Проверка плавности анимации скролла в связке с подсветкой текста.
